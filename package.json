{
  "name": "bullet-heist",
  "version": "0.1.0",
  "description": "Top-down ricochet puzzle game",
  "private": true,
  "type": "module",
  "scripts": {
    "dev":   "npx serve . --listen 3000",
    "editor": "npx serve . --listen 3001",
    "serve": "npx serve .",
    "build": "node scripts/build.js",
    "validate:levels": "node scripts/validate-levels.js"
  },
  "keywords": ["game", "ricochet", "puzzle", "canvas"],
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "serve": "^14.2.3"
  }
}
